# Simplified Dagster instance config using default local (sqlite) storage.
run_coordinator:
  module: dagster.core.run_coordinator
  class: QueuedRunCoordinator
# You can run the dummy job with:
# dagster job execute -f repo.py -j simple_job
